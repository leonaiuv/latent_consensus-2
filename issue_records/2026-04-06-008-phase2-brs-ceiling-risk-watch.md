# 问题记录 008：`EXP-B01` 首次验证已满分，需警惕 BRS 主线重复出现天花板效应

## 所属阶段

- `Phase 2` BRS 主线

## 触发背景

`EXP-B01`（`CoT` 基线）在首次完整验证后，`val` 集结果为：

- `exact_match = 1.0`
- `step_accuracy = {2: 1.0, 4: 1.0, 6: 1.0}`
- 样本数：`1200`（`400/400/400`）

## 风险判断

这与 `Phase 1 Arithmetic-Debug` 中出现的“验证集过早学满”模式高度相似，存在以下研究风险：

1. 若 `test` 与 `ood` 也接近满分，则 BRS 可能同样缺少区分 `LC/Ind` 的空间
2. 若继续无条件顺跑 `B02 ~ B12`，可能在 `8-18` 小时后才确认整个矩阵都缺少判别力
3. 一旦 `macro_acc_id` 普遍饱和，主结论会被迫转移到 `macro_acc_ood`，甚至需要先回头提高数据难度

## 当前处理策略

1. 先只放行 `EXP-B01`
2. 等 `test/ood` 正式结果出来后，再决定是否继续后续 `11` 个实验
3. 已在运行层加守护：
   - 若 `EXP-B01/summary.json` 出现，则立即中断主梯子，防止自动进入 `EXP-B02`
   - 若意外开始创建 `EXP-B02` 目录，也立即中断

## 后续判定规则

1. 若 `ood_exact_match` 也接近 `1.0`：
   - 暂停 `B02 ~ B12`
   - 优先提高 BRS 数据难度，再重新进入主线
2. 若 `ood_exact_match` 明显低于饱和区：
   - 允许继续 B 组实验
   - 同时把后续主比较指标优先放在 `macro_acc_ood`

## 当前状态

- 状态：已确认
- 优先级：高

## 追加结果（2026-04-06）

已按“停止继续训练，直接使用当前 best checkpoint 做 `test/ood` 评估”的方式完成 `EXP-B01` 收尾，最终结果为：

- `val_exact_match = 1.0`
- `test_exact_match = 1.0`
- `ood_exact_match = 1.0`
- `id_step_accuracy = {2: 1.0, 4: 1.0, 6: 1.0}`
- `ood_step_accuracy = {2: 1.0, 4: 1.0, 6: 1.0}`

这说明：

1. `BRS` 在当前正式实现下已经重复出现与 `Arithmetic` 相同的天花板效应风险
2. 当前不应继续顺跑 `B02 ~ B12`
3. 下一步更合理的方向是先提高 BRS 难度，再重新进入主线矩阵
