inherits: local_base.yaml

experiment:
  phase: phase2
  name: brs_main

data:
  dataset: brs
  steps: [2, 4, 6]
  train_per_step: 4000
  val_per_step: 400
  test_per_step: 400
  ood_per_step: 400

training:
  # 正式规模 BRS 的 OOD step6 文本长度实测最高约 247 token，需给答案区保留余量。
  seq_len: 256
